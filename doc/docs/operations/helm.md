
# Helm chart

The repository includes Helm assets under `helm/`.

## Goals

- Deploy backend and frontend to Kubernetes
- Configure environment variables and secrets
- Optionally expose ingress and TLS

## Next steps

This page is intentionally a scaffold.

If you want production-grade Helm docs, expand with:

- value schema overview
- example `values.yaml` per environment
- secret management approach (SOPS/ExternalSecrets/etc.)
- ingress examples (nginx, HAProxy, OpenShift routes)
