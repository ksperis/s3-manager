image:
  backend:
    repository: ghcr.io/ksperis/kaelo-backend
    tag: "0.2.0"
    pullPolicy: IfNotPresent
  frontend:
    repository: ghcr.io/ksperis/kaelo-frontend
    tag: "0.2.0"
    pullPolicy: IfNotPresent

backend:
  replicas: 1
  databaseType: postgresql
  # Existing Secret is mandatory. It must contain database-url, ui-jwt-keys,
  # api-jwt-keys, credential-keys and internal-cron-token.
  existingSecret: ""
  secretKeys:
    databaseUrl: database-url
    uiJwtKeys: ui-jwt-keys
    apiJwtKeys: api-jwt-keys
    credentialKeys: credential-keys
    internalCronToken: internal-cron-token
    smtpPassword: smtp-password
  env:
    APP_ENV: production
    API_V1_PREFIX: /api
    PUBLIC_ORIGIN: https://kaelo.local
    CORS_ORIGINS: '["https://kaelo.local"]'
    ALLOWED_HOSTS: '["kaelo.local"]'
    REFRESH_TOKEN_COOKIE_SECURE: "true"
    REQUIRE_REGISTERED_S3_LOGIN_ENDPOINTS: "true"
    SEED_SUPER_ADMIN_MODE: disabled
    WEBAUTHN_ORIGIN: https://kaelo.local
    WEBAUTHN_RP_ID: kaelo.local
    # No implicit storage seed is created in production. Configure endpoints
    # administratively after startup or provide an explicit HTTPS seed.
    APP_SETTINGS_PATH: /data/app_settings.json
    BILLING_DAILY_RETENTION_DAYS: "365"
    QUOTA_HISTORY_HOURLY_RETENTION_DAYS: "30"
    QUOTA_HISTORY_DAILY_RETENTION_DAYS: "365"
  extraEnv: []
  persistence:
    enabled: true
    size: 1Gi
    storageClass: ""
    accessMode: ReadWriteOnce

frontend:
  replicas: 1
  env:
    CSP_CONNECT_SRC: "'self'"
  extraEnv: []

service:
  backend:
    type: ClusterIP
    port: 8000
  frontend:
    type: ClusterIP
    port: 80

postgresql:
  enabled: false
  image:
    repository: postgres
    tag: "16-alpine"
    pullPolicy: IfNotPresent
  auth:
    username: kaelo
    password: ""
    database: kaelo
  service:
    port: 5432
  persistence:
    enabled: true
    size: 5Gi
    storageClass: ""

ingress:
  enabled: true
  className: ""
  annotations: {}
  host: kaelo.local
  tls:
    - secretName: kaelo-tls
      hosts:
        - kaelo.local

billingCronJob:
  enabled: false
  schedule: "0 2 * * *"
  dayOffset: 1
  backoffLimit: 1
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  extraEnv: []

healthcheckCronJob:
  enabled: false
  schedule: "*/5 * * * *"
  backoffLimit: 1
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  extraEnv: []

quotaMonitorCronJob:
  enabled: false
  schedule: "0 * * * *"
  backoffLimit: 1
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  extraEnv: []

usageHistoryCronJob:
  enabled: false
  schedule: "0 3 * * *"
  backoffLimit: 1
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 2
  extraEnv: []

nameOverride: ""
fullnameOverride: ""
