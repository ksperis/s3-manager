{{- if .Values.healthcheckCronJob.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "bucketreef.fullname" . }}-healthcheck
  labels:
    {{- include "bucketreef.labels" . | nindent 4 }}
    app.kubernetes.io/component: healthcheck-cron
spec:
  schedule: {{ .Values.healthcheckCronJob.schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: {{ .Values.healthcheckCronJob.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.healthcheckCronJob.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      backoffLimit: {{ .Values.healthcheckCronJob.backoffLimit }}
      template:
        metadata:
          labels:
            {{- include "bucketreef.labels" . | nindent 12 }}
            app.kubernetes.io/component: healthcheck-cron
        spec:
          restartPolicy: OnFailure
          automountServiceAccountToken: false
          securityContext:
            {{- toYaml .Values.cronJobs.podSecurityContext | nindent 12 }}
          containers:
            - name: healthcheck-runner
              image: "{{ .Values.image.backend.repository }}:{{ .Values.image.backend.tag }}"
              imagePullPolicy: {{ .Values.image.backend.pullPolicy }}
              securityContext:
                {{- toYaml .Values.cronJobs.containerSecurityContext | nindent 16 }}
              resources:
                {{- toYaml .Values.cronJobs.resources | nindent 16 }}
              volumeMounts:
                - name: tmp
                  mountPath: /tmp
              env:
                - name: HEALTHCHECK_API_BASE
                  value: {{ printf "http://%s-backend:%d" (include "bucketreef.fullname" .) (.Values.service.backend.port | int) | quote }}
                - name: INTERNAL_CRON_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: {{ .Values.backend.existingSecret | quote }}
                      key: {{ .Values.backend.secretKeys.internalCronToken | quote }}
                {{- range .Values.healthcheckCronJob.extraEnv }}
                - name: {{ .name }}
                  value: {{ .value | quote }}
                {{- end }}
              command: ["/bin/sh", "-c"]
              args:
                - |
                  python - <<'PY'
                  import os
                  import sys
                  import requests

                  base = os.environ.get("HEALTHCHECK_API_BASE")
                  token = os.environ.get("INTERNAL_CRON_TOKEN")
                  if not base or not token:
                      print("Missing HEALTHCHECK_API_BASE or INTERNAL_CRON_TOKEN", file=sys.stderr)
                      sys.exit(1)
                  url = f"{base}/api/internal/healthchecks/run"
                  resp = requests.post(url, headers={"X-Internal-Token": token}, timeout=30)
                  print(f"Healthcheck returned HTTP {resp.status_code}")
                  resp.raise_for_status()
                  PY
          volumes:
            - name: tmp
              emptyDir: {}
{{- end }}
