{{- if .Values.usageHistoryCronJob.enabled }}
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ include "s3-manager.fullname" . }}-usage-history
  labels:
    {{- include "s3-manager.labels" . | nindent 4 }}
    app.kubernetes.io/component: usage-history-cron
spec:
  schedule: {{ .Values.usageHistoryCronJob.schedule | quote }}
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: {{ .Values.usageHistoryCronJob.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.usageHistoryCronJob.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      backoffLimit: {{ .Values.usageHistoryCronJob.backoffLimit }}
      template:
        metadata:
          labels:
            {{- include "s3-manager.labels" . | nindent 12 }}
            app.kubernetes.io/component: usage-history-cron
        spec:
          restartPolicy: OnFailure
          containers:
            - name: usage-history-collector
              image: "{{ .Values.image.backend.repository }}:{{ .Values.image.backend.tag }}"
              imagePullPolicy: {{ .Values.image.backend.pullPolicy }}
              env:
                - name: USAGE_HISTORY_API_BASE
                  value: {{ printf "http://%s-backend:%d" (include "s3-manager.fullname" .) (.Values.service.backend.port | int) | quote }}
                {{- $backendToken := get .Values.backend.env "INTERNAL_CRON_TOKEN" }}
                {{- if $backendToken }}
                - name: INTERNAL_CRON_TOKEN
                  value: {{ $backendToken | quote }}
                {{- else if .Values.usageHistoryCronJob.internalToken }}
                - name: INTERNAL_CRON_TOKEN
                  value: {{ .Values.usageHistoryCronJob.internalToken | quote }}
                {{- end }}
                {{- range .Values.usageHistoryCronJob.extraEnv }}
                - name: {{ .name }}
                  value: {{ .value | quote }}
                {{- end }}
              command: ["/bin/sh", "-c"]
              args:
                - |
                  python - <<'PY'
                  import os
                  import sys
                  import requests

                  base = os.environ.get("USAGE_HISTORY_API_BASE")
                  token = os.environ.get("INTERNAL_CRON_TOKEN")
                  if not base or not token:
                      print("Missing USAGE_HISTORY_API_BASE or INTERNAL_CRON_TOKEN", file=sys.stderr)
                      sys.exit(1)
                  url = f"{base}/api/internal/usage-history/collect"
                  resp = requests.post(url, headers={"X-Internal-Token": token}, timeout=30)
                  print(resp.status_code, resp.text)
                  resp.raise_for_status()
                  PY
{{- end }}
